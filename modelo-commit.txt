# ARQUIVO MODELO PARA ESCREVER COMMIT

FALTA FAZER
USER:
REQUISITO 04 - 400 E 401

+++++++++++++

modelo-commit
git commit -m "texto" -m "descricao"

------------------------------------------------

> COMMIT REALIZADOS
git commit -m "Atualização do projeto" -m "Mudança na estrutura do projeto, pasta api > src. Rota até o useController está funcionando."

git commit -m "User CRUD e BD" -m "Criação do banco de dados. User CRUD (Falta alguns response status)"

git commit -m "Category CRUD e BD, sem requisito 01" -m "Category CRUD, falta o requisito 01 (Falta alguns response status). Criação do banco de dados."
----------------

FEITO
+ DESCRIÇÃO COMMIT +
Criação da rota homepage.
privatesRoutes
userRoutes
userController

Criação do banco de dados.



http://localhost:3000/v1/category
/search?limit=-1&page=1&fields=name,slug&use_in_menu=true

ex.:
/search?fields=name,slug

const query = request.query;

let data = query.fields;

atributes: [data]  // [name, slug]

























